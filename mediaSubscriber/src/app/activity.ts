export default class Activity {
    title: string;
    color: string;
    type: string;

    constructor(title, color, type) {
        this.title = title;
        this.color = color;
        this.type = type;
    }
}
